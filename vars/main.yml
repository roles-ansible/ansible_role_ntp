---
playbook_version_number: 2065
ntp__playbook_version_path: 'role-ntp_chaos-bodensee_github.com.version'

ntp__vars:
  files:
    - "{{ ansible_facts['os_family'] }}.yml"
    - 'fallback.yml'
  paths:
    - 'vars'

ntp__install:
  files:
    - "setup-{{ ansible_facts['system'] }}.yml"
    - 'setup-Linux.yml'
  paths:
    - 'tasks/packages'
